{
  "name": "agent",
  "description": "Multi-worktree orchestrator: branch parallel work into git worktrees, spawn focused Claude agents per worktree via a dedicated zellij workers session, coordinate reviews and CI fixes",
  "author": {
    "name": "dev360"
  }
}
